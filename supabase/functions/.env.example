# Copy to supabase/functions/.env (local) or set with `supabase secrets set` (production).
SYNC_SECRET=long-random-string
REVALIDATE_URL=http://host.docker.internal:3000/api/revalidate
REVALIDATE_SECRET=same-value-as-apps/web
GITHUB_TOKEN=
