# Read by the Supabase CLI (config.toml `env(...)` values). Copy to supabase/.env.
# GitHub OAuth app for LOCAL admin login.
# Callback URL on GitHub: http://127.0.0.1:54321/auth/v1/callback
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=replace-me
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=replace-me
